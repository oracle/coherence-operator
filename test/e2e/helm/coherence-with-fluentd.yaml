apiVersion: coherence.oracle.com/v1
kind: CoherenceCluster
metadata:
  name: test-cluster
spec:
  logging:
    level: 9
    fluentd:
      enabled: true
  roles:
  - role: storage
    replicas: 2
  - role: proxy
    replicas: 1
