apiVersion: coherence.oracle.com/v1
kind: CoherenceCluster
metadata:
  name: test-cluster
spec:
  logging:
    level: 9
    fluentdEnabled: true
  roles:
  - role: storage
    replicas: 2
  - role: proxy
    replicas: 1
