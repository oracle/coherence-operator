apiVersion: coherence.oracle.com/v1
kind: CoherenceCluster
metadata:
  name: test-cluster
spec:
  replicas: 2
  coherence:
    image: ${TEST_COHERENCE_IMAGE}
  coherenceUtils:
    image: ${UTILS_IMAGE}
