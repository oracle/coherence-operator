apiVersion: coherence.oracle.com/v1
kind: CoherenceDeployment
metadata:
  name: status-ha-exec-test
spec:
  role: storage
  replicas: 2
  readinessProbe:
    initialDelaySeconds: 10
    periodSeconds: 10
  scaling:
    probe:
      exec:
        command: [ "/utils/runner", "version" ]
