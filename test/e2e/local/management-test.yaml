apiVersion: coherence.oracle.com/v1
kind: CoherenceDeployment
metadata:
  name: management
spec:
  replicas: 1
  coherence:
    management:
      enabled: true
      port: 30000
  ports:
    - name: management
      port: 30000
