apiVersion: coherence.oracle.com/v1
kind: CoherenceCluster
metadata:
  name: test-cluster
spec:
  roles:
    - role: data
      network:
        hostname: "foo.com"
    - role: proxy
      network:
        hostname: "bar.com"
