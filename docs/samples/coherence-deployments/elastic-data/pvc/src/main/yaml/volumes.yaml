store:
  volumeMounts:
  # mount point in container
  - mountPath: /elastic-data
    name: elastic-data-volume
  volumes:
  - name: elastic-data-volume
    hostPath:
      # directory location on host
      path: /coh/elastic-data
      type: DirectoryOrCreate
