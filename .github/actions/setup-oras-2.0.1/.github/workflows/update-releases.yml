# Copyright The ORAS Authors.
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Update releases.json

on:
  schedule:
    # Daily at 06:00 UTC.
    - cron: '0 6 * * *'
  workflow_dispatch:
    inputs:
      version:
        description: 'Optional ORAS version to add (e.g. 1.3.2). Leave blank to auto-detect new releases.'
        required: false
        default: ''

permissions:
  contents: read

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node 24.x
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Update releases.json
        id: update
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            node .github/scripts/update-releases.mjs "${{ inputs.version }}"
          else
            node .github/scripts/update-releases.mjs
          fi

      - name: Rebuild dist
        if: steps.update.outputs.ADDED_VERSIONS != ''
        run: npm run build

      - name: Create pull request
        if: steps.update.outputs.ADDED_VERSIONS != ''
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: automated/update-releases
          delete-branch: true
          sign-commits: true
          commit-message: 'chore: update releases.json for ${{ steps.update.outputs.ADDED_VERSIONS }}'
          title: 'chore: update releases.json for ${{ steps.update.outputs.ADDED_VERSIONS }}'
          body: |
            Automated update adding ORAS release(s) `${{ steps.update.outputs.ADDED_VERSIONS }}`
            to `src/lib/data/releases.json`, with `dist/` rebuilt to match.

            Generated by `.github/workflows/update-releases.yml` via
            `.github/scripts/update-releases.mjs`.

            Review checklist:
            - Checksums match the upstream release(s) at
              https://github.com/oras-project/oras/releases (the one thing not
              covered by CI)
            - `check-dist` is green — confirms `dist/` matches the rebuilt source

            Refs #168.
          signoff: true
